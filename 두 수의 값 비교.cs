using System;

public class Solution
{
    // 두 수의 값 비교
    public int solution(int num1, int num2)
    {
        if (num1 != num2) // num1과 num2가 같지 않다면
            return -1; // -1 반환
        else // 아니라면
            return 1; // 1 반환
    }
}
